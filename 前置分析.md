# JobCheck V0.2.0 完成報告

> 狀態：已完成並合併至 `main`  
> 完成日期：2026-09-12  
> Unity：2022.3.62f3 LTS  
> 封版測試：265 passed、0 failed

## 1. 版本目標

V0.2.0 的主要目標是將 V0.1「Unity UI 直接讀寫零散 JSON」的架構，重構為可驗證、可追蹤且可安全搬移的資料流程。

```text
Unity UI
    ↓
Command / Query
    ↓
Domain + Validator + ApplicationEvent Reducer
    ↓
Mapper
    ↓
DTO + Repository
    ↓
data/*.json
```

## 2. 已完成範圍

### Domain

- Company、JobPosting、Application、ApplicationEvent 模型。
- 穩定 ID 產生器與必要欄位驗證。
- Application 以事件歷程推導目前狀態。
- 同名公司只做嚴格正規化比對，不做 AI 或模糊合併。
- 重複投遞建立新 Application，可連結最近一筆前次投遞。

### Persistence 與 migration

- V0.2 DTO、Mapper、JSON serializer 與 Repository。
- 讀寫時執行單體與跨模型驗證，失敗時不回傳半套資料。
- V0.1 demo 來源備份、SHA-256、staging、manifest 與 report。
- V0.1 demo 已轉換為 20 Company、20 JobPosting、1 Application 與 1 MigrationSnapshot。
- 日常新增、編輯與 Application 事件寫入服務已完成。

### Unity UI

- V0.2 資料列表、篩選、詳情與原始職缺內容。
- 新增及編輯職缺。
- 應徵狀態、收藏、備註、追蹤日期與事件歷程。
- Tag 與 RiskFlag 使用受控字串清單，可新增、編輯、保存及中文顯示。
- TextMesh Pro 統一使用 Yozai-Light SDF，並啟用中文動態 atlas。

## 3. Demo 與資料安全

- `data/` 只放虛構 demo，不是真實求職資料。
- V0.1 migration 原始資料及證據保留於 `data/migration/`。
- V0.1 執行期回退已退役，頂層 `jobs/`、`job_tracking/`、`job_index/` 已移除；Unity 正式流程只讀寫 V0.2 `data/`。
- V0.2 UI 人工驗收資料已整理成正式 demo，包含同公司多職缺、Saved Application、Tag 與 RiskFlag 情境。
- 新產生的 UUID JSON 預設只留本機；只有 `.gitignore` allowlist 中的正式 demo 納入 Git。

## 4. 驗收結果

- Unity 2022.3.62f3 可正常編譯。
- EditMode Test Runner：265 passed、0 failed、0 skipped。
- 人工確認列表顯示、新增、編輯、狀態選擇、追蹤日期、事件歷程、中文字體與 Tag/RiskFlag 流程。
- V0.2.0 所有功能分支已合併至 `main`。

## 5. 已知技術債

以下不阻擋 V0.2.0 封版：

- `AllJobPage` 與 `Panel_JobDetail` 責任過多，可在後續版本拆分。
- 尚未驗證獨立 Build 的資料存放路徑；目前封版基準是 Unity Editor MVP。

## 6. 不屬於 V0.2.0

- 真實職缺平台匯入。
- 漏斗、平台成效與結案原因分析頁。
- 新版匯入／匯出。
- AI Fit Score、摘要、面試準備與自動化。

這些分別屬於 V0.2.1～V0.2.4，不再擴張 V0.2.0 範圍。

